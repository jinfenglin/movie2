  To test the files, please run the main above the lib directory. Because the 'require' clause 
  are all wrote in the formate like './lib/file_I_want_to_include.rb'
  
  The code are mostly in the lib directory, while there are several tests. Simple run the command:
  $rake test
  
  The result generated by code climate do not contain the document file produced by rdoc, because
  these jsp file will detriment the gpa.